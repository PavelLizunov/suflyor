# Logs
logs
*.log

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.sw?

# Rust / Cargo build artifacts (~3 GB) + visual-test PNGs
target/

# Slint crate build artifacts
slint-experiment/target/
slint-experiment/verify*.png
slint-experiment/overlay-host.*.log

# Phase B1 — overlay-backend crate build artifacts
overlay-backend/target/
overlay-backend/Cargo.lock

# Autonomous mode internal state (hook-managed, machine-local)
.claude/autonomous_active
.claude/_progress_counter

# Local session journals + recordings (may contain transcript secrets)
sessions/
*.jsonl

# OS noise
Thumbs.db
desktop.ini

# Python recovery scripts I cooked in temp (don't commit these)
recover-config.py
update-groq.ps1
check-config.ps1
fix-bom.ps1
enable-debrief.ps1
reset-snippets.ps1
check-state.ps1
