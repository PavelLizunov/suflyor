# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust / Cargo build artifacts (~3 GB)
src-tauri/target/

# Tauri generated bindings (rebuilt automatically)
src-tauri/gen/

# Slint pilot crate build artifacts (Phase 0)
slint-experiment/target/

# Autonomous mode internal state (hook-managed, machine-local)
.claude/autonomous_active
.claude/_progress_counter

# Local session journals + recordings (may contain transcript secrets)
sessions/
*.jsonl

# OS noise
Thumbs.db
desktop.ini

# Python recovery scripts I cooked in temp (don't commit these)
recover-config.py
update-groq.ps1
check-config.ps1
fix-bom.ps1
enable-debrief.ps1
reset-snippets.ps1
check-state.ps1
