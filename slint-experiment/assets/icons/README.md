# Icon convention

- SVG canvas: `viewBox="0 0 16 16"`.
- Outlined icons: `fill="none"`, `stroke-width="1.6"`, round caps and joins.
- Keep the drawing centered with roughly 1.5 px of outer breathing room.
- Use a distinct icon when actions have different meanings; color is applied by Slint.
