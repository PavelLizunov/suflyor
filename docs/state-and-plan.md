# Suflyor — состояние и план (после v0.5.0, 2026-05-30)

Живой документ для продолжения работы / после компакта контекста.

> **АКТУАЛЬНО на 2026-06-25 (вечер) — фиксы по live-отчёту тестера (F1–F8)**, поверх
> v0.22.1, НЕ релизено (копим до явного «релизь»). Источник целей:
> `docs/archive-panel-fixes.md` (5 целей). Статус:
> - ✅ **F2** (краш удаления) — исправлен ранее, тестер подтвердил.
> - ✅ **F1** (плеер: точный seek) — закоммичен `56a0c0a`. Persist `audio_ms`
>   end-to-end (journal→indexer→models→SQLite миграция 0004→`line_start_offset_ms`
>   предпочитает audio_ms, старые сессии — fallback). Убирает СЕКУНДНОЕ отставание
>   STT-финализации (жалоба тестера). Ревью нашёл: НЕ sample-perfect — остаётся
>   СУБ-СЕКУНДНЫЙ остаток (гранулярность чанка ~200мс + меж-канальный skew записи:
>   рекордер пишет каждый канал от своего первого чанка, не от общей эпохи). В пределах
>   «доли секунды допустимы». Sample-perfect = общая эпоха захвата + WAV-паддинг (живой
>   аудио-пайплайн, НЕ проверить без микрофона, риск регресса записи) — ОТЛОЖЕНО до живой
>   проверки тестера. Доки честно переформулированы (не «accurate», а «much closer»).
> - 🔄 **F3/F4/F5** — реализованы, гейт идёт (`bnblau4u7`), далее ревью-агент + коммит:
>   - F3: ↻ при существующей сводке (conspect на диске) → in-app подтверждение. Новый
>     `conspect::exists` (+тест); рефактор ↻-хендлера в Rc-замыкание `start_resummary`;
>     проперти/коллбэки `confirm-resummary-*` / `resummary-confirmed|cancelled` +
>     оверлей-зеркало delete-confirm в archive.slint.
>   - F4: ↻ НЕактивна у сессий без данных (нет записи И нет транскрипта) + подсказка
>     «Недостаточно данных». `ArchiveRow.has-data` = `has_recordings || transcript_lines>0`.
>   - F5: ▶/⏸ плеера + чекбокс реплики → SVG (`play/pause/check.svg`, иконки бара) вместо
>     эмодзи/текстового «✓» (transcript.slint, центрирующий layout).
>   - i18n: 4 новые строки в `slint-replay.po` (Recreate summary?/…/Not enough data).
> - ✅ **F6** — УЖЕ корректно в коде (transcript.slint 252/376 + open_transcript ставит
>   `empty`/`has_audio`): «есть транскрипт, нет записи»→«Аудио не сохранено»; «нет
>   транскрипта»→«Транскрипт не сохранён». Тестер 1.9 = OK* (сессия БЕЗ транскрипта →
>   корректно «Транскрипт не сохранён»). КОД НЕ МЕНЯЛ — тестеру перепроверить кейс
>   «есть транскрипт, нет записи».
> - ✅ **F7** — закрыто (debug-артефакт; на release задержки нет).
> - 📋 **F8** (Настройки→«Память» тормозит/краш у одного юзера) — ОТДЕЛЬНЫЙ инкремент
>   (после F3/F4/F5). Диагноз: `reload_memory` (settings_memory.rs:30, на КАЖДОЕ открытие
>   Настроек) гонит ДВА unbounded-запроса (нет LIMIT) синхронно в UI-потоке +
>   Slint рендерит НЕ-виртуализованными for-циклами → тяжёлый юзер пересекает порог
>   ресурсов (OOM/abort) = краш; остальные = фриз. Фикс: LIMIT+count к
>   `list_candidates`/`list_memory_items` + reload в воркер-поток (`invoke_from_event_loop`,
>   паттерн уже есть) + защитный декод строк. Индексы 0003 уже покрывают запросы.
> - ФИНАЛ: release-сборка + HTML-чеклист тестеру (вкл. СВЕЖУЮ запись для F1), релиз по «релизь».
>
> ---
> *(ниже — более ранний раунд ТЗ1–2b того же дня, для истории)*

> **АКТУАЛЬНО на 2026-06-25 — «Архив → панель работы с созвонами»** (HEAD после
> релиза **v0.22.1**; НЕ релизено — копим до явного «релизь»). По ТЗ тестера (4
> доработки архива) сделаны **ВСЕ 4 из 4**, каждый инкремент: гейт 5 слоёв +
> состязательный ревью-агент (0 critical / 0 important) + флаг **VISUAL/AUDIO
> CHECK PENDING** (нужны глаза/уши до релиза):
> - ✅ **ТЗ1 Стенограмма** — новое окно `transcript.slint` (📄 в строке архива):
>   структурированный read-only список (время · Система/Микрофон · текст), пустая
>   сессия → «Транскрипт не сохранён», per-line чекбоксы + «выделить все»,
>   «Копировать всё» / «Копировать выбранное» (+ галка «с таймкодами»), локальный
>   буфер UTF-8. Окно process-lifetime в `WindowRegistry` (стелс на лайв-тогл +
>   переживает закрытие архива). Чистая `tile_copy::format_transcript_for_copy`.
>   Коммиты `7b57812`→`8523cac`.
> - ✅ **ТЗ2а Удаление** — 🗑 в строке + нативное подтверждение → hard-delete всех
>   артефактов (.jsonl / recordings / conspect) + каталог (каскад). Новый
>   `overlay-backend/src/session_admin.rs`. Коммит `8deba9c`.
> - ✅ **ТЗ3 Ретро-резюме** — кнопка ↻ Summary теперь и для сессий БЕЗ аудио
>   (источник: транскрипт каталога → jsonl user_prompt, с дедупом). Новый
>   `summary_source.rs`. Коммит `61f87f2`.
> - ✅ **ТЗ2б Плеер** — мини-плеер в окне стенограммы: клик по строке → воспроизведение
>   микса с её момента; play/pause; click-to-seek seek-bar; «mm:ss / mm:ss»; подсветка
>   активной строки; нет записи → плеер скрыт + «Аудио не сохранено». Движок **rodio**
>   (решение #4; `default-features=false`; **cargo-deny advisories/bans/sources OK**) —
>   UI-thread thread-local `transcript_player`; seek = рестарт `Arc<[i16]>`-курсора;
>   позиция через `Instant`; 200ms `slint::Timer` пушит прогресс/время/актив-строку.
>   `session_audio::{sample_for_ms,ms_for_sample,session_has_recordings}`. Коммиты
>   `0b7fdb1`(даунмикс)→`18b15ed`(маппинг)→`ea3bc69`(движок)→`09538e2`(UI).
>   **ЖИВАЯ аудио-фича**: гейт без аудио-устройства звук НЕ подтверждает (юнит-тест =
>   только маппинг ms↔сэмпл) → обязательна живая проверка на слух перед релизом.
> Отдельно: `ArchiveWindow` сам имеет ТУ ЖЕ пред-существующую дыру стелс-тогла (тоже
> не в `WindowRegistry`) — заведено отдельной фоновой задачей, не в объёме ТЗ.
>
> **HEAD = v0.8.2** (2026-06-01, commit `b195c4d`). Разделы ниже описывают
> состояние на v0.5.2 и НЕ обновлялись по ходу v0.6–v0.8. Актуальная карта
> последней волны и скаута багов — в `docs/scout-report-v0.8.2.md`. Кратко по
> релизам: v0.6.0 = F8 vision-скриншот; v0.7.0 = стелс без мигания тайла;
> v0.8.0 = устойчивость (видимость ошибок · перезапуск 🔄 · эскалация в облако
> 🧠); v0.8.1 = «липкая» облачная эскалация; v0.8.2 = скаут-фиксы
> (бар не врёт про AI · лимит на облачные продолжения · RU-оверфлоу бара).

## Где мы
- **Выпущен v0.5.2** (Latest) с NSIS-инсталлятором; авто-апдейтер его предлагает.
  Репо: github.com/PavelLizunov/suflyor. v0.5.2 = вкладка **«🩺 Диагностика»**
  (готовность AI/STT/mic/sys/stealth + «Проверить всё»; системный звук —
  **самотест**: приложение само проигрывает ambient-сигнал в вывод и ловит его
  loopback'ом, вшитый клип `overlay-backend/assets/diag-chime.s16`) + светлый
  водяной знак на тёмной теме + перевод хоткеев. Полный мастер первого запуска —
  backlog #132. v0.5.1 = F6-фикс + пин бара + импорт серверных настроек + чистка
  легаси-доков. v0.5.0 = новый бренд + «+ тайл».
- **WASAPI render добавлен** (`audio::play_test_clip`) тем же `wasapi`-крейтом, что
  и захват — render-петля симметрична `record_source_until_stop`. Без новых зависимостей.
- Стек: чистый Rust + Slint. Две crate-ы:
  - `slint-experiment/` — бинарь `overlay-host` (UI в `ui/*.slint`, оркестрация
    в `src/bin/overlay_host.rs`).
  - `overlay-backend/` — audio/stt/ai/runtime/config/journal/kb (без UI).
  - Сборка: `cargo build --release --bin overlay-host` (из slint-experiment).
    Инсталлятор: `scripts/build-slint-release.ps1 -Installer`.
- Юзер на **локальном AI** (gemma @ 127.0.0.1:8080) + GigaAM STT; тема **Light Frost**
  (color_scheme=3). 2 монитора: основной ландшафт 1920×1080, вторичный ПОРТРЕТ
  1200×1920 при x=-1200.

## Сделано в этой сессии (всё закоммичено + запушено, вошло в v0.5.0)
- **Бренд:** WebP→SVG (potrace) → `assets/icon.svg` (белая заливка + `colorize`
  под тему). Лого на баре 40px (accent, **тянет окно**), бледный вотермарк на фоне
  Настроек, иконка приложения (.ico/.png) обрезана до ~96% кадра (крупнее в трее/
  на ярлыке). `icon.png`/`.ico` — для трея/окна; `icon.svg` — внутри UI.
- **Slint-баги (из аудита):** F4 Enter открывает верхний результат (Up/Down
  невозможно — std LineEdit съедает стрелки); код-блоки в тайлах `no-wrap`+`clip`;
  whisper bearer очищается после Save.
- **i18n:** починена захардкоженная RU-строка плейсхолдера транскрипта (теперь
  `@tr` + запись в `ru.po`). Переключение RU/EN работает (`select_bundled_translation`
  + `ui_language` персистится).
- **«+ тайл»:** переписан — тайл спавнится **мгновенно** (прямой `TileWindow::new`)
  + реальный AI-запрос через `cfg.ai_endpoint(false)` (резолвит локал/облако).
  Пусто → тайл-подсказка; ошибка AI → тайл с ошибкой.

## План (что дальше)

### Из трёх доков (ВСЕ закрыты)
1. ✅ **#125 Импорт серверных настроек** (98bf73e) — `config::merge_server_settings`
   (чистая, без IO) + `import_server_settings_from` (read+merge+save) копируют ТОЛЬКО
   AI/STT-поля серверов; локальные профили/устройства/UI/хоткеи/сниппеты сохраняются.
   Кнопка «Import server settings…» в Настройках + RU-перевод + юнит-тест + ревью-агент
   (чисто, `[]`).
2. ✅ **#126 Чистка легаси-доков** (7e28e07) — CLAUDE.md / architecture.md /
   REVIEW_AGENT_PROMPT переписаны под Slint; ADR-001 + security-audit получили
   SUPERSEDED/HISTORICAL баннеры (история сохранена).
3. ✅ Третий док `slint-surface-audit.md` — закрыт частично ранее (F4 Enter, код-блоки,
   таблицы); остаток (markdown-ссылки, заголовок Replay, ширина бара на 125-150% DPI)
   — в backlog ниже.

### Баги (статус)
3. ✅ **СДЕЛАНО (8f8cfe5)** — бар прыгал между мониторами. Причина: у бара НЕ было
   своей логики позиционирования; Slint/winit ставил его на дефолтный монитор.
   Фикс: пин на ПЕРВИЧНЫЙ монитор при старте (в `apply_overlay_hwnd`, рядом с
   transparency+stealth). Smoke: «bar pinned to primary at (420, 24)». Тянуть
   мышью по-прежнему можно (пин одноразовый, при старте).
4. ✅ **СДЕЛАНО (d8e7c47)** — F6 «не срабатывал». Причина: НЕ регистрация (smoke
   показал `F6 hotkey registered`), а `manual_spawn_tile` бил в ОБЛАКО вместо
   `ai_endpoint(false)` → для локального юзера AI-запрос молча падал, а тайл в этом
   пути появляется только ПОСЛЕ ответа AI; пусто/ошибка слали `tile:error` без
   UI-хендлера. Фикс: резолвер + видимый фидбэк-тайл на пусто/ошибку (generic, без
   утечки base_url).
5. **Проверить «+ тайл» / F6 вживую с активной сессией** — проверен только мгновенный
   спавн + ветка «нет транскрипта». Ветка с реальным ответом по транскрипту кодово
   верна и endpoint доступен, но вживую не подтверждена. F6 теперь идёт через тот же
   резолвер — стоит проверить оба заодно (нажать F6 при идущей сессии → тайл с
   ответом локальной gemma).

### Ниже приоритет (из slint-surface-audit)
6. Широкие таблицы клипаются (нет h-scroll); markdown-адаптер теряет ссылки/
   emphasis; заголовок окна Replay «(Phase 0)»; ширина бара на 125-150% DPI;
   `overlay_host.rs` ~5k строк (кандидат на разбиение).
7. **#76** Settings: Card/Row titled sections (опциональный полиш).

## Ключевые подводные камни (для будущих сессий)
- **Проверять Slint-оверлей через `CopyFromScreen` по rect HWND** (Win32
  EnumWindows+GetWindowRect по pid+title → PNG → Read). Скриншоты computer-use
  ВРУТ про цвета прозрачного оверлея (показывают тёмным на светлой теме) и не
  показывают бар, когда он на вторичном мониторе. `zoom` — в физических координатах
  экрана (1920×1080), не в координатах скриншота.
- **Бар переезжает сам / прыгает между мониторами** → синтетические MoveWindow+клики
  ненадёжны; ширина статус-пилюли сдвигает x чипов; спавн тайла rate-limited.
  Хоткеи: F4=палитра (работает), F6=ручной спавн (не срабатывает), F7=collapse-заглушка.
- **AI endpoint:** `cfg.ai_endpoint(false)` резолвит локал/облако по `ai_provider`;
  сырой `c.ai_base_url` — ВСЕГДА облако (молча висит/падает для локальных юзеров).
- **i18n:** строки в `@tr()` — это английский msgid; `translations/ru/LC_MESSAGES/
  slint-replay.po` (плоские msgid/msgstr, без msgctxt) держит русский. Захардкоженная
  кириллица обходит механизм.
- **Иконки:** potrace в `%TEMP%\potrace`; `icon-source.png` — прозрачный line-art;
  генерация через `scripts/gen_icon.ps1`; SVG в Slint работает по умолчанию — НЕ
  добавлять `svg` в features slint (ломает выбор версии).
- **Релиз:** поднять версию в `slint-experiment/Cargo.toml` + `scripts/slint-installer.nsi`
  (PRODUCT_VERSION); `build-slint-release.ps1 -Installer`; `gh release create vX.Y.Z
  <installer> --title ... --notes-file ...`.
- Не коммитить `nini-context-backup.txt` (личная подготовка юзера — держать локально).
