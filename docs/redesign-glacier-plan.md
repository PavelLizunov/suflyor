# Редизайн «Glacier» — HTML-референс → Slint (план переноса)

_Источник: `C:\Users\x3d_mutant\AppData\Local\Temp\suflyor - Редизайн.html` (визуальный референс, НЕ реализация). Анализ 2026-06-20, 5 агентов по поверхностям. Только анализ — код не менялся._

## 0. Главное
- **Цвета макета = дефолтная (Glacier) схема `Theme` 1:1.** Все 13 ролей (bg-*, text-*, accent #4C8DFF, success/warning/danger, border*) уже в `theme.slint:22-42`. Менять нечего.
- Редизайн — это **единая система**: один синий акцент, RU-статусы, **состояние = цвет + слово**, смысловые группы, карточки, шкала высот 28/36/44, радиусы 999/12/8, полезные пустые состояния.
- Шрифты IBM Plex **НЕ берём** (не подключены) — остаются Segoe UI / Cascadia Mono. Mono-подписи рендерим Cascadia.
- `Metrics` global уже существует (`metrics.slint`) — новые размерные токены кладём туда.

## 1. Токены

| Токен макета | Статус | Что делать |
|---|---|---|
| bg-base/surface/elevated, text-primary/secondary/muted, accent/accent-hover, success/warning/danger, border/border-strong (13 шт.) | ✅ совпадают | Ничего. Уже = дефолтной схеме. |
| **`on-accent` #06101F** (текст/глиф НА заливке акцентом) | 🆕 новый | Добавить `Theme.on-accent`, **по значению на каждую из 4 схем** (Glacier — тёмный; Light-Frost — светлый; Graphite/Obsidian — тёмный, т.к. акценты светлые). Проверить AA. |
| **`accent-subtle` #13233D** (плашка-подложка active/selected/badge) | 🆕 новый | Добавить `Theme.accent-subtle`, **литерал на каждую из 4 схем** (~12-18% accent над surface). Заменить нынешние `accent.with-alpha(...)`. |
| **`radius-pill` 999** | 🆕 новый | `Metrics.radius-pill: 999px`. NB: реальное визуальное изменение vs нынешних 10px чипов. |
| **`radius-field` 8** | 🆕 новый | `Metrics.radius-field: 8px` (сейчас radius-control=10). |
| **высоты 36 / 44** | 🆕 новые | Шкала 28/36/44; 28 уже есть (control-lg). Добавить 36 и 44 (НЕ перезаписывать существующие 20/22, которые в ходу). ⚠ любая высота, текущая в `apply_bar_size`/preferred-size — вне рамок (ограничение 4). |
| `bg-sunken` #0A0D12 (фон кода) | ♻ есть похожий | Это роль `Theme.code-bg` (#090C10). Переиспользовать, не плодить новый (можно чуть подвинуть значение). |
| IBM Plex Sans/Mono | ⛔ не берём | Ограничение 6. Остаётся Segoe UI / Cascadia Mono. |

## 2. Проверка жёстких ограничений (всё учтено)
- **#1 Компакт = TTS-панель** — из HTML-компакта берём ТОЛЬКО стилистику статус-пилюли (цвет=состояние) и грип. Структуру (спросить/захватить/таймер) НЕ берём. ✅
- **#2 Грип** — сохраняется: полный бар `overlay_bar.slint:591`, компакт-плашка, сайдбар настроек `:609`, шапка архива `:111`. ✅
- **#3 «Сводка» в архиве** — ПРОВЕРЕНО: `archive.slint:276` гейтит по `row.has-recordings` (ставится в Rust `aux_windows.rs:901` из наличия записей), НЕ по числу строк. **Не трогаем гейт**, только стиль пилюли. ✅
- **#4 Размеры окон Slint↔Rust** — синхронизированы и **вне визуального прохода**: бар 1200×64 (`apply_bar_size`), тайл 460 (`TILE_DEFAULT_W/H`), визард 640×480 + архив 720×540 (Rust-парные в aux_windows). Можно менять только в Slint (без Rust): **настройки 720×600**, **палитра 620→520**, **окно вопроса 560→430** (Rust-констант нет). ✅
- **#5 Без Rust/Win32-логики** — все строки = рескин/перегруппировка существующих контролов, колбэки те же. Вынесено за рамки (требует Rust): текст «реплик N · ответов M» + цвет статуса архива (строятся в Rust), инлайн-акцент в тексте тайла (нужна модель markdown). ✅
- **#6 Шрифты** — без IBM Plex. ✅  **#7 Ассеты** — `brand-head.svg`/`brand-duo-compact.svg`/`icons/*.svg`. ✅  **#8** — только анализ. ✅

## 3. Таблица «макет → Slint → изменения» (по поверхностям)

### Бар (полный) — `overlay_bar.slint`
| Макет | Сейчас | Изменение | Объём |
|---|---|---|---|
| Статус-пилюля: иконка + **точка-состояние** + RU-метка; idle=серый, live=зелёный+glow, rec=красный мигающий | status-pill `:56x`, метки из Rust | Добавить цветную точку перед меткой; idle="ожидание" нейтральный (не зелёный) | M |
| Смысловые **группы** в «grp»-обёртках с разделителями | верхний HBox `:552` | Обернуть кластеры чипов в Rectangle-«grp» | M ⚠ширина бара фикс. 1200 |
| icon-кнопки .ib 28×28 r999; пилюли .pill h28 r999 | Chip h22 r10, RecordBtn h22 r10 | Поднять до 28 + r-pill | M ⚠h22→28 меняет высоту строки → задевает окно 64px (огр.4) |
| таймер mono warning-tinted, только в сессии; **`$0.000` убран** | Timer Chip `:663`, cost Chip | Таймер в Cascadia + warning; убрать cost-чип | S |
| нижняя строка **совпадает с состоянием** | нижний HBox `:857` | Сообщение+точка футера красятся под статус | M |
| **один синий акцент** | ~14 сырых hex по чипам | Свести к `Theme.accent` | L (механически) |

### Компакт + TTS-пилюля — `overlay_bar.slint` (БОЛЬШИНСТВО УЖЕ СДЕЛАНО)
| Макет | Сейчас | Изменение | Объём |
|---|---|---|---|
| Состояния готово/читает/пауза цветом | `tts-status-kind` + map (уже есть!) | Сверить RU-слова + цвета; добавить волну-эквалайзер у «читает» (опц.) | S |
| грип в компакте | плашка целиком — драг | Опц. вернуть видимый грип-значок (огр.2) | S |

### Настройки — `settings_panel.slint`
| Макет | Сейчас | Изменение | Объём |
|---|---|---|---|
| **карточки .scard** (плотные вкладки) | AI-мост `:1645` плоский VBox | Новый компонент ScardCard, обернуть группы (это #7 плотность) | **L** |
| .seg сегмент-контрол (4B/12B) | твин-кнопки `:1851` | Заменить на единый сегмент | M |
| .tgl тумблер 38×22 | DarkCheck 34×18 | Подрасти трек/кноб | S |
| .ibtn.accent (заливная) + .inp h36 | std LineEdit/Button | Высоты 36 + заливная первичная кнопка (нужен `on-accent`) | M |
| .closebtn (полноширинная) | std Button `:722` | Рескин (ASCII X оставить) | S |
| пустая вкладка: тумблер + .empty-превью | Коучинг `:810` текст | Добавить превью-блок (статичные чипы) | M |
| фон-иллюстрация убрана на плотных | `:742` уже скрыта | Подтверждено — готово | S |

### Визард — `wizard.slint`
| Макет | Сейчас | Изменение | Объём |
|---|---|---|---|
| 2 колонки [рейл шагов | тело] | один VBox `:71` | Перестроить root в HBox + левый рейл с кружками-шагами (done=зелёный, cur=accent) | **L** ⚠размер 640×480 не менять (Rust-парный) |
| .wcard карточки выбора | 3 кнопки `:96` | Рескин в карточки (accent-subtle при выборе), колбэк тот же | M |
| caption(mono)+h3+desc, футер Назад/Пропустить/Далее | уже близко | Mono-caption + сверить | S |

### Архив — `archive.slint`
| Макет | Сейчас | Изменение | Объём |
|---|---|---|---|
| шапка: icon-chip + «Архив сессий» + count-pill | `:104` (= драг-зона) | Добавить chip + count-пилюлю, **драг сохранить** (огр.2) | M |
| строка: точка-статус + mono-заголовок + meta + «Сводка» | `:226` | Добавить явную точку (цвет из Rust); рескин | M ⚠текст/цвет из Rust |
| «Сводка» = accent-subtle пилюля | `:276` (гейт по has-recordings) | Только стиль; **гейт не трогать** (огр.3) | S |

### Рабочие — `tile.slint` / `palette.slint` / `text_ask.slint`
| Макет | Сейчас | Изменение | Объём |
|---|---|---|---|
| **тайл: шапка = 4 действия + «⋯» overflow** | `:149` показывает все | Свести к copy/speaker/pin/x + меню (огр.5: колбэки те же) | **L** |
| пустой тайл (из архива) | нет ветки | Добавить empty-state | M |
| код-блок bg-sunken | `:502` (Theme.code-bg) | Уже совпадает по роли | S |
| палитра: короткий placeholder + mono-подсказка + accent-subtle выбор | `:94/:165` | Короткий placeholder + строка-хинт; ширина 620→520 (Slint-only) | M |
| окно вопроса 430, поле притушено | text_ask | Почти совпадает; ширина 560→430 (Slint-only) | S |

## 4. Поэтапный план (визуал/Slint + токены, без Rust)
- **Этап 0 — Токены.** `theme.slint`: `on-accent` + `accent-subtle` (по 4 схемы, AA). `metrics.slint`: `radius-pill 999`, `radius-field 8`, высоты 36/44. Визуально пока ничего не меняется (фундамент). Гейт.
- **Этап 1 — Компакт/TTS (пилот, тебе важен).** Состояния готово/читает/пауза + опц. волна-эквалайзер + грип. Малый объём (база уже есть). **Показать тебе** → дальше.
- **Этап 2 — Бар полный.** Состояние=цвет (точка+футер), группы, один акцент, таймер mono, убрать cost. ⚠Высоты чипов 22→28 — ОТДЕЛЬНО (задевает окно 64px → нужна парная Rust-синхронизация ИЛИ оставить 22). Цвет/группы/акцент — безопасно.
- **Этап 3 — Настройки.** scard-карточки (#7) + seg + tgl + заливная кнопка + closebtn + пустая-превью. Размер не трогаем.
- **Этап 4 — Архив.** Точка-статус + «Сводка»-пилюля (стиль) + chip-шапка. Гейт/тексты Rust — не трогаем.
- **Этап 5 — Визард.** Рейл + кружки + wcard. Размер 640×480 не трогаем.
- **Этап 6 — Рабочие.** Тайл: шапка 4+overflow + empty-state. Палитра/вопрос: ширины (Slint-only) + хинты.

Каждый этап: свой 5-слойный гейт + ревью + живой смок. Размеры окон, текст статусов из Rust, markdown-инлайн — вне этого прохода (помечены).

## 5. Решения, которые нужно подтвердить
1. **Высоты чипов бара 22→28** конфликтуют с фикс. окном 1200×64 (огр.4). Варианты: (а) оставить 22px, взять только стиль; (б) поднять до 28 + парная Rust-правка высоты бара (выходит за «без Rust»). 
2. **`accent-subtle`/`on-accent` на 4 схемы** — подобрать значения для Graphite/Obsidian/Light-Frost (не только Glacier), с проверкой контраста.
3. Порядок: предлагаю **Этап 0 → Этап 1 (компакт)** как пилот, дальше по одному.
