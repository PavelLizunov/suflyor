# GOAL — suflyor: редактируемая память + код-рендер (3 ТЗ тестера, 2026-07-02)

## Цель
Закрыть 3 ТЗ тестера ОДНИМ выверенным релизом (накопительно, без марафона):
(1) редактируемая память + захват в память из ответов/стенограммы; (2) код от
ЛОКАЛЬНОЙ модели, оформленный как у облака; (3) копия на каждом код-блоке.
Решения согласованы с fable — заземление и разбор в
`docs/feature-requests.md` §«Набор ТЗ … 2026-07-02».

## Объём и порядок (B → C → A1 → A2)
- [x] **B — локальный код в fenced-блок (ПРОМПТ, гейт 1–4).** ✅ commit `5c670c1` (ci 0/0 + ревью 0/0). Live-проверка на слабой локальной — за тестером.
      Инлайн-предписание `code` активно уводит слабую локальную модель в инлайн.
      Переписать в ТРЁХ answer-промптах: «инлайн `code` — только для коротких команд
      в строке; многострочный код/конфиги/SQL/YAML — ТОЛЬКО fenced-блоком с языком».
      Сайты: `overlay-backend/src/ai.rs:859` (`build_request` — F9/PTT/vision),
      `overlay-backend/src/runtime.rs:1086` (`build_auto_tile_prompts` — авто-тайлы
      по детекту вопроса, ОСНОВНОЙ live-путь) И
      `slint-experiment/src/bin/overlay_host/tile_followup.rs:92`
      (`followup_system_prompt` — «Продолжить диалог»). `summary_*`-промпты
      (`runtime.rs:329/533`) НЕ трогаем (проза, plain-text). Живая проверка
      «локальная реально даёт блок» — на тестере/юзере.
- [ ] **C — копия на каждом код-блоке (UI, гейт 1–5).**
      В `tile.slint`, ветка `if block.kind == 5`: кнопка + callback
      `copy-block-clicked(string)` с `block.text` (уже чистый код) → `clipboard-win`;
      фидбек «Скопировано» на нужном блоке. `kind`-дискриминанты НЕ меняем (синхрон
      с `markdown_spike.slint` не задет). Общая «копия всего» остаётся. Глиф — НЕ
      голый ✓ (skia = квадрат).
- [ ] **A1 — редактирование одобренной памяти (UI, гейт 1–5).**
      Строка айтема (`settings_panel.slint`): кнопка «Редактировать» → инлайн
      `LineEdit` (свойство `editing-item-id`) → «Сохранить»/«Отмена». Хендлер зовёт
      `Store::update_memory_item_text` (УЖЕ есть, `sqlite_store.rs:544`) +
      `reload_memory` (уже `pub(crate)`). Новые строки → `@tr` + `.po`.
- [x] **A2 — «⭐ В память» из ответа/стенограммы (UI, гейт 1–5).** ✅ тайлы `829be76` + стенограмма `d7f71f8` (общий `insert_approved_note`).
      Кнопка на блоке тайла/саммари (та же ветка цикла, что кнопка C) и на строке
      стенограммы → открывает редактор A1 (обрезать/переформулировать) →
      `insert_memory_item` (approved, kind `note`). БЕЗ настройки режима (YAGNI).
      Факт уже доходит до ИИ и «Продолжить диалог» (`context_for_meeting`). Новые
      строки → `@tr` + `.po`. НЕ произвольное выделение (Slint `Text` не тянет).

## Критерии приёмки (тестерские)
- **B:** код/команды локальной модели = оформленный fenced-блок, как облако; в
  «Продолжить диалог» — тоже; сырых ``` в готовом блоке не видно.
- **C:** у каждого код-блока своя «Копировать» (чистый код без backticks); общая
  копия работает; фидбек «Скопировано»; одинаково local/cloud.
- **A1:** у одобренного факта есть «Редактировать» + «Удалить»; правка сохраняется,
  «Отмена» — нет; переживает перезапуск.
- **A2:** во фрагменте тайла/саммари/стенограммы можно добавить в память (с правкой
  перед сохранением); факт идёт в ИИ и «Продолжить диалог»; авто-извлечение работает.

## Гейт (каждый пункт — отдельным атомарным коммитом)
`cargo fmt` → `scripts/ci.ps1` (0/0 ×3 крейта, incl. i18n_guard) → независимый
ревью-агент по диффу (`docs/REVIEW_AGENT_PROMPT.md`, 0 critical / 0 important) →
коммит (хук `git-gate.ps1` блокирует). UI-пункты (C/A1/A2) дополнительно: live-смоук
+ PrintWindow на глифы + UI-ревью .slint-диффа.

## Стоп-условия и релиз
- Стоп на пункте при: critical в ревью / ci не 0/0 / незапланированный риск →
  ДИАГНОСТИРОВАТЬ, не долбить вслепую.
- Накопить B + C + A1 + A2 в ОДИН релиз (≈ v0.26.0). НЕ билдить инсталлер и НЕ
  релизить без явного «релизь»; НЕ пушить без «запуш».
- Перед релизом: `docs/retest-*v0.26.0*.html` (хук требует) + версия в
  `slint-experiment/Cargo.toml` И `scripts/slint-installer.nsi` (синхронно).

## Статус
- [x] Заземление по коду (3 агента) + согласование с fable.
- [x] B (`5c670c1`) → [x] C (`3b25dfa`) → [x] A1 (`3e60569`) → [x] A2 (тайлы `829be76` + стенограмма `d7f71f8`) → [ ] pre-release визуал-пасс (Slint-MCP/PrintWindow + фидбэк-тост A2) → [ ] retest-*v0.26.0*.html → релиз по «релизь».

**ВСЕ 4 ПУНКТА РЕАЛИЗОВАНЫ И ЗАКОММИЧЕНЫ (master, НЕ запушено, НЕ релиз).** 5 коммитов, каждый: ci.ps1 0/0 ×3 крейта + независимый adversarial-ревью 0 critical / 0 important. UI live-визуал (C/A1/A2) — в пред-релизном пассе. Отложенный микро-UX: тост «сохранено» для A2 (решить по живому виду).
