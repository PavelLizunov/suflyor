[package]
name = "suflyor-macos-gate0b"
version = "0.0.0"
edition = "2021"
publish = false

[features]
ui-mcp = ["slint/mcp"]

[build-dependencies]
cc = "1"
slint-build = "1.17"

[dependencies]
slint = "1.17"

[lints.clippy]
expect_used = "deny"
panic = "deny"
unwrap_used = "deny"
